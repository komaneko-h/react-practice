## react

index.html
↓
main.tsx
↓
App.tsx
の順番で読み込まれる

## 基本構成

```
function App() {
  // JavaScriptのコードを書く
  
  return (
   /* HTMLで画面の見た目部分を書く */
  )
}

export default App
```

## データ型チェック

MPAではよくDBから取得したデータとの型不一致が起こるとエラーになるがTypeScriptはあくまでコンパイル時に方チェックをする。つまりAPIで取得するデータの方はコンパイル時にチェックされない。これをチェックするためにはJSやTSで一つずつチェックする必要がある。さすがにそれはめんどいからZodというライブラリが使われるらしい。知らんかった。そもそもJSにもデータ型は存在したみたい。

## strictmode

開発時に副作用を検知しやすくする。
例えばコンポーネント関数内でalert処理を書くと２回アラートが表示されるなど。これやるとコンポーネントが呼ばれるたび（再レンダリング）にアラートが出たりする。画面遷移時にアラートを出したい場合はuseEffectを使うらしい。これでも２回出るらしいけど。詳細はまた今度

## Tips

export defaultの場合は読み込み先で自由に名前変えれる。可読性考えるとnamed export({対象})とかの方がいいと思う。